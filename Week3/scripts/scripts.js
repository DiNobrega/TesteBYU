
document.querySelectorAll('.cta-button').forEach(button => {
    button.addEventListener('click', () => {
        alert('You clicked a button! This can be linked to booking functionality.');
    });
});
